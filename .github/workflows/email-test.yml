name: Email Step Test

on:
  push:
    branches:
      - master

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: 24f2005847@ds.study.iitm.ac.in
        run: echo "Hello, world!"
